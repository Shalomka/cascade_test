name: cascade_http
description: package:http transport adapter for the cascade acceptance-test harness.
version: 0.2.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  cascade_core:
    path: ../cascade_core
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^11.0.0
