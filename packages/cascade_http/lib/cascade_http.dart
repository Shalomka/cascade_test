/// cascade_http: the `package:http` transport adapter for the cascade
/// acceptance-test harness. Provides a registry-backed `MockClient`.
library;

export 'src/http_installer.dart';
export 'src/registry_mock_client.dart';
